[pytest]
testpaths = tests
pythonpath = .
addopts = -q --strict-markers -m "not integration"
markers =
    integration: exercises the real checkpoint from MODEL_PATH (run with `pytest -m integration`)
